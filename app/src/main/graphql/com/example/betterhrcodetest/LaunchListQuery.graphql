query LaunchListQuery($offset: Int, $limit: Int){
    launches(offset: $offset, limit: $limit) {
        id
        links {
            mission_patch
            mission_patch_small
        }
        mission_id
        mission_name
        launch_date_utc
    }
}
